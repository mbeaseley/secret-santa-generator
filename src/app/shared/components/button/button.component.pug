.cc-button
  ng-template(#content)
    ng-content

  a(
    *ngIf="button?.uiRef"
    tabindex="0"
    role="button"
    attr.href="{{button.uiRef}}"
    attr.aria-label="{{button.description || null}}"
    attr.title="{{button.description || null}}"
    ngClass="{{button.displayClasses}}"
  )
    ng-container(*ngTemplateOutlet="content")

  button(
    *ngIf="!button?.uiRef"
    type="{{button.type}}"
    ngClass="{{button.displayClasses}}"
    attr.aria-label="{{button.description || null}}"
    attr.title="{{button.description || null}}"
  )
    ng-container(*ngTemplateOutlet="content")